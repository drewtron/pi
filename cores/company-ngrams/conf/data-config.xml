<?xml version="1.0" encoding="UTF-8" ?>
<dataConfig>
  <!--
      DataImportHandler for Mongo
      https://github.com/james75/SolrMongoImporter
  -->
  <dataSource name="Mosaic" type="MongoDataSource" database="mosaic" host="${MONGO_HOST}" username="${MONGO_UID}" password="${MONGO_PWD}" />
  <document name="Companies">
    <entity name="company"
            processor="MongoEntityProcessor"
            query="{}"
            collection="organizations"
            datasource="Mosaic"
            transformer="MongoMapperTransformer" >

    </entity>
  </document>
</dataConfig>
